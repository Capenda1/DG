import type { ReactNode } from "react";

export const metadata = {
  title: "Restaurar imagem · Admin",
};

export default function RestaurarImagemLayout({
  children,
}: {
  children: ReactNode;
}) {
  return children;
}
