import type { ReactNode } from "react";

/** Perfis com papel Cliente — rotas sob `/conta`. */
export default function ClientRoutesLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
