# CORS — origens permitidas (vírgula). Vazio = sem CORS externo (só rede interna).
# CORS_ORIGINS=http://localhost:3000

# Chave opcional — se definida, pedidos exigem cabeçalho X-Api-Key
# AI_SERVICE_API_KEY=
