# API Nest — Dádiva Go

Código da API em TypeScript (NestJS + Prisma + PostgreSQL).

- **Configurar e correr:** ver [`../../docs/SETUP.md`](../../docs/SETUP.md) (secção API).
- **Contexto da pasta `backend/`:** [`../README.md`](../README.md).

Comandos habituais (na pasta `backend/api/`): `npm install`, `npm run prisma:generate`, `npm run prisma:migrate`, `npm run start:dev`.
